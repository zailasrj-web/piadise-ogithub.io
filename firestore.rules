rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura solo si el usuario ha iniciado sesión.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}